node_modules/
*.log
# The rotated generation the logger keeps (see LOG_OLD_FILE in src/plugin.js).
# `*.log` does not match a name ending in `.old`.
*.log.old
local-assets/
usage-cache.json
product/
.superpowers/
docs/superpowers/
.playwright-mcp/
hook-secret
hook-token
# Machine-local Claude Code approvals. Only a global ~/.gitignore_global was covering
# this, which does not travel with a clone or reach CI.
.claude/settings.local.json
